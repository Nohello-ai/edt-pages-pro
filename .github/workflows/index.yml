name: Build ARM64 Kernel for DroidVM

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Linux kernel version (e.g., 6.12.8)'
        required: true
        default: '6.12.8'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y git bc bison flex libssl-dev make \
            gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu \
            crossbuild-essential-arm64 libelf-dev dwarves ccache

      # ---------- 源码缓存 ----------
      - name: Cache kernel source
        uses: actions/cache@v4
        with:
          path: linux-*
          key: kernel-src-${{ github.event.inputs.kernel_version }}
          restore-keys: |
            kernel-src-${{ github.event.inputs.kernel_version }}

      # ---------- ccache 缓存（restore）----------
      - name: Restore ccache
        uses: actions/cache/restore@v4
        with:
          path: ~/.ccache
          key: ccache-droidvm-${{ github.event.inputs.kernel_version }}-${{ github.run_number }}
          restore-keys: |
            ccache-droidvm-${{ github.event.inputs.kernel_version }}-

      # ---------- 下载源码 ----------
      - name: Download kernel source
        run: |
          KERNEL_VERSION=${{ github.event.inputs.kernel_version }}
          if [ ! -d "linux-${KERNEL_VERSION}" ]; then
            wget -q https://cdn.kernel.org/pub/linux/kernel/v${KERNEL_VERSION%%.*}.x/linux-${KERNEL_VERSION}.tar.xz
            tar -xf linux-${KERNEL_VERSION}.tar.xz
          fi
          echo "KERNEL_VERSION=${KERNEL_VERSION}" >> $GITHUB_ENV

      # ---------- 配置内核 ----------
      - name: Configure kernel
        run: |
          cd linux-${KERNEL_VERSION}
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- defconfig

          if [ -f "../kernel-config.txt" ]; then
            echo "Applying kernel-config.txt"
            while IFS= read -r line || [ -n "$line" ]; do
              line=$(echo "$line" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
              [[ -z "$line" || "$line" =~ ^# ]] && continue
              if [[ "$line" =~ ^CONFIG_([^=]+)=(.*)$ ]]; then
                opt="${BASH_REMATCH[1]}"
                val="${BASH_REMATCH[2]}"
                if [[ "$val" == "y" ]]; then
                  ./scripts/config --enable "$opt"
                elif [[ "$val" == "m" ]]; then
                  ./scripts/config --module "$opt"
                elif [[ "$val" == "n" ]]; then
                  ./scripts/config --disable "$opt"
                elif [[ "$val" =~ ^\".*\"$ ]]; then
                  ./scripts/config --set-str "$opt" "$(echo "$val" | sed 's/^"//;s/"$//')"
                elif [[ "$val" =~ ^[0-9]+$ ]]; then
                  ./scripts/config --set-val "$opt" "$val"
                else
                  ./scripts/config --set-str "$opt" "$val"
                fi
              fi
            done < "../kernel-config.txt"
          fi

          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

          echo "=== Key config check ==="
          grep "^CONFIG_VIRTIO_CONSOLE=y" .config && echo "[OK] VIRTIO_CONSOLE" || echo "[FAIL] VIRTIO_CONSOLE"
          grep "^CONFIG_VIRTIO_NET=y" .config && echo "[OK] VIRTIO_NET" || echo "[FAIL] VIRTIO_NET"
          grep "^CONFIG_DEFAULT_BBR=y" .config && echo "[OK] DEFAULT_BBR" || echo "[FAIL] DEFAULT_BBR"
          grep "^CONFIG_IO_URING=y" .config && echo "[OK] IO_URING" || echo "[FAIL] IO_URING"
          grep "^CONFIG_TCP_CONG_BBR=y" .config && echo "[OK] BBR" || echo "[FAIL] BBR"

      # ---------- 编译内核 ----------
      - name: Build kernel
        run: |
          export PATH=/usr/lib/ccache:$PATH
          ccache -M 5G
          ccache -z
          cd linux-${KERNEL_VERSION}
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- -j$(nproc)
          mkdir -p ../artifacts

      - name: Show ccache stats
        run: ccache -s

      # ---------- 保存 ccache ----------
      - name: Save ccache
        uses: actions/cache/save@v4
        with:
          path: ~/.ccache
          key: ccache-droidvm-${{ github.event.inputs.kernel_version }}-${{ github.run_number }}

      # ---------- 打包（未压缩 Image）----------
      - name: Package artifacts
        run: |
          V=${{ env.KERNEL_VERSION }}
          cp linux-${V}/arch/arm64/boot/Image artifacts/vmlinuz-${V}-droidvm
          cp linux-${V}/vmlinux artifacts/vmlinux-${V}-droidvm.debug
          cp linux-${V}/.config artifacts/config-${V}-droidvm

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ env.KERNEL_VERSION }}
          path: artifacts/

      # ---------- Release ----------
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Kernel ${{ env.KERNEL_VERSION }} (Build ${{ github.run_number }})
          tag_name: v1.${{ github.run_number }}
          files: |
            artifacts/vmlinuz-${{ env.KERNEL_VERSION }}-droidvm
            artifacts/vmlinux-${{ env.KERNEL_VERSION }}-droidvm.debug
            artifacts/config-${{ env.KERNEL_VERSION }}-droidvm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
